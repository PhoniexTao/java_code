<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.phoniex.mybatis.mapper.UserInfoMapperXML">
    <resultMap id="BaseMap" type="com.phoniex.mybatis.model.UserInfo">
        <id property="id" column="id"></id>
        <result property="deleteFlag" column="delete_flag"></result>
        <result property="updateTime" column="update_time"></result>
    </resultMap>
    <sql id="sql">
        select * from user_info
    </sql>
    <select id="selectAll" resultType="com.phoniex.mybatis.model.UserInfo">
        <include refid="sql"></include>
<!--        SELECT id,username,`password`,age,gender,phone,delete_flag AS deleteFlag,create_time AS createTime,update_time AS updateTime FROM user_info-->
    </select>
    <select id="selectAll2" resultMap="BaseMap">
        select * from user_info
    </select>
    <insert id="insertUser">
        insert into user_info (username,`password`,age) VALUES (#{username},#{password},#{age})
    </insert>
    <insert id="insertUser2" useGeneratedKeys="true" keyProperty="id">
        insert into user_info (username,`password`,age) VALUES (#{userInfo.username},#{userInfo.password},#{userInfo.age})
    </insert>
    <insert id="insertUser3">
        insert into user_info
        <trim prefixOverrides="," suffixOverrides="," prefix="(" suffix=")">
            <if test="username">
                username,
            </if>
            <if test="password">
                `password`,
            </if>
            <if  test="age">
                age,
            </if>
            <if test="gender != null">
                gender,
            </if>
            <if test="phone != null">
                phone
            </if>
        </trim>

        VALUES
        <trim prefixOverrides="," suffixOverrides="," prefix="(" suffix=")">
            <if test="username != null">
                #{username},
            </if>
            <if test="password != null">
                #{password},
            </if>
            <if test="age != null">
                #{age},
            </if>
            <if test="gender != null">
                #{gender},
            </if>
            <if test="phone != null">
                #{gender},
            </if>
            <if test="phone != null">
                #{phone}
            </if>
        </trim>
    </insert>
    <update id="updateUser">
        update user_info set password = #{password}, age = #{age} where id = #{id}
    </update>
    <delete id="deleteUser">
        delete from user_info where id = #{id}
    </delete>
    <select id="selectByCondition" resultType="com.phoniex.mybatis.model.UserInfo">
        select * from user_info
        <where>
            <if test="phone != null">
                and phone = #{phone}
            </if>
            <if test="deleteFlag != null">
                and delete_flag = #{deleteFlag}
            </if>
        </where>

<!--        <trim prefixOverrides="and">-->
<!--            <if test="phone != null">-->
<!--                and phone = #{phone}-->
<!--            </if>-->
<!--            <if test="deleteFlag != null">-->
<!--                and delete_flag = #{deleteFlag}-->
<!--            </if>-->
<!--        </trim>-->

<!--        <trim prefixOverrides="and">-->
<!--            <if test="phone != null">-->
<!--                phone = #{phone}-->
<!--            </if>-->
<!--            <if test="deleteFlag != null">-->
<!--                and delete_Flag = #{deleteFlag}-->
<!--            </if>-->
<!--        </trim>-->
    </select>
    <update id="updateByConfition">
        update user_info
        <set>
            <if test="password != null">
                password = #{password},
            </if>
            <if test="age != null">
                age = #{age},
            </if>
            <if test="gender != null">
                gender = #{gender}
            </if>
        </set>
        where id = #{id}
<!--        set-->
<!--        <trim suffixOverrides=",">-->
<!--            <if test="password != null">-->
<!--                password = #{password},-->
<!--            </if>-->
<!--            <if test="age != null">-->
<!--                age = #{age},-->
<!--            </if>-->
<!--            <if test="gender != null">-->
<!--                gender = #{gender}-->
<!--            </if>-->
<!--        </trim>-->

    </update>
    <delete id="batchDelete">
        DELETE FROM user_info where id in
<!--        ( 23,22,21,20);-->
        <foreach collection="ids"  open="(" close=")" separator="," item="id">
            #{id}
        </foreach>

<!--        id = 1 or id = 2 or id = 3-->
<!--        <foreach collection="ids" separator="or" item="id">-->
<!--            id = #{id}-->
<!--        </foreach>-->

<!--        ("z1","p1"),("z2","p2"),("z3","p3")-->
<!--        <foreach collection="userinfos" separator="," item="userinfo">-->
<!--            (#{userinfo.username},#{userinfo.password})-->
<!--        </foreach> -->
    </delete>


</mapper>
