<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.phoniex.mybatis.mapper.ArticleInfoMapper">

    <select id="selectArticleInfoById" resultType="com.phoniex.mybatis.model.ArticleInfo">
        SELECT ta.*,tb.username,tb.age
        FROM article_info ta
        LEFT JOIN user_info tb
        on ta.uid = tb.id
        WHERE ta.id = #{id};
    </select>
</mapper>
